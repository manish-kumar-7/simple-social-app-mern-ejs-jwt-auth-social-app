<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Document</title>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
       <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block"> Logout</a>
    </div>
    <div class="flex item-start gap-3">
      <div class="w-10 h-10  rounded-md overflow-hidden">
        <img src="/images/uplodes/<%= user.profilepic%>" class="w-full h-full object-cover" alt="pic">
      </div>
    <h3 class="text-3xl mb-5"> Hello 👍, <%=user.name%></h3>
    </div>
   
     <a href="/profile/upload" class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40">Update Profile Pic</a>
    
    <h5 class="mb-5 mt-3">You can create a post</h5>
    <form action="/post" method="post">
      <textarea name="content" class="p-3 outeline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="Write What is in your mind"></textarea>
      <input type="submit" class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40 " value="create new post" >
    </form>
    <div class="posts mt-20">
      <h3 class="text-zinc-400">Your post</h3>
      <div class="postcontainer mt-5">
     
        <% user.post.reverse().forEach((post)=>{%>
             <div class="post w-1/3 p-4 mb-2 border-2 rounded-md bg-zinc800 border-zinc-700">
          <h4 class="text-blue-500 mb-2">@<%=user.username%></h4>
          <p class="text-sm tracking-tight"><%=post.content%></p>
          <small class="mt-4 inline-block"><%= post.likes.length%></small>
          <div class="btns flex gap-10 mt-1">
            <a href="/like/<%= post._id%>" class="text-blue-500">
              <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
            </a>
            <a href="/edit/<%= post._id%>" class="text-red-600">Edit Your Post</a>
          </div>
        </div>
       <%})%>

       

      </div>
    </div>
  </div>
</body>
</html>